{
  "name": "tokutei-ginou-saas-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check:deps": "dependency-cruiser --config .dependency-cruiser.cjs apps/api/src"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.5.4",
    "dependency-cruiser": "^16.4.2"
  },
  "packageManager": "npm@10.9.8",
  "engines": {
    "node": ">=20"
  }
}
